git add .
git commit -m "Cap nhat"
git push

npm run dev

lệnh upload:

    git init
    git add .
    git commit -m "Cap nhat"
    git branch -M main
    git remote add origin https://github.com/ptdatazz/Toeic_V2.git
    git push -u origin main

    git pull origin main --allow-unrelated-histories

    git add .
    git commit -m "Merge"
    git push -u origin main